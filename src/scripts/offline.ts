/**
 * Offline detection & UI — INJECTED via Rust at document end.
 * Listens to online/offline events, shows/hides a fixed banner.
 * Also periodically hides Facebook's native offline dialogs via CSS.
 * See src-tauri/src/lib.rs OFFLINE_BANNER_JS and OFFLINE_DIALOG_JS constants.
 */
export {};
